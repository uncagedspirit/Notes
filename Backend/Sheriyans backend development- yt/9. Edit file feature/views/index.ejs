<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tasks</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <h1 class="text-6xl font-semibold p-4">TASKS</h1>
        
        <div class=" flex flex-col items-center">
            <h3 class="text-2xl font-bold ">Create new task</h3>
            
            <div class="form text-white p-12 w-1/2">
                <form action="/create" method="post" class="flex flex-col items-center">
                    <input name="title" type="text" placeholder="Task title here" class="block px-5 py-2 bg-zinc-800 rounded-lg w-full outline-none">
                    <textarea name="details" placeholder="task details..." class="block px-5 py-2 bg-zinc-800 resize-none mt-4 rounded-lg w-full outline-none"></textarea>
                    <input type="submit" value="Create Task" class="bg-blue-300 mt-6 p-2 rounded-lg outline-none">
                </form>
            </div>

            <div class="tasks p-10 flex flex-wrap justify-center gap-4">
                <% if(files.length > 0){ %>
                    <% files.forEach(function(val){ %>
                        <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-xl">

                            <h1 class="text-white text-xl tracking-tight">
                                <%= val.split('-').join(' ').replace('.txt', '') %>
                            </h1>

                            <div class="flex w-full mt-3 justify-between">
                                <a class="text-blue-300 inline-block " href="/file/<%= val %>">
                                    Read more...
                                </a>
                                <a class="text-zinc-400" href="/edit/<%= val %>">
                                    edit title
                                </a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <h3 class="text-2xl text-zinc-400">
                        No tasks yet.
                    </h3>
                <% } %>
            </div>
        </div>
    </div>
    
</body>
</html>